flask==2.3.2
scikit-learn==1.3.0 # Keeping this for now, but will try to fix the warning later if this works.
pandas==2.0.3
numpy==1.24.3
gunicorn==21.2.0
flask-cors==4.0.0
requests==2.31.0
joblib==1.5.1 # Ensure joblib is explicitly listed and compatible